---
title: CTA Section with Avatars
description: Modern CTA with avatars and smooth reveal animations.
---

<BlocksPreview
  name='cta-section-with-avatars'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/cta-sections/cta-section-with-avatars
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge lucide-react
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Avatar Group`](/docs/components/avatar-group), [`Stripe Button`](/docs/components/stripe-button)</Step>

</Steps>
</TabsContent>
</BlockTabs>