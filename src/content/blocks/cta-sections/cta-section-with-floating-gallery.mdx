---
title: CTA Section With Floating Gallery
description: CTA section with staggered image gallery.
---

<BlocksPreview
  name='cta-section-with-floating-gallery'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
```bash value="pkg"
npx shadcn@latest add @scrollxui/cta-sections/cta-section-with-floating-gallery
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>
```bash value="deps"
npm install motion clsx tailwind-merge class-variance-authority lucide-react
```

<Step>Add util file</Step>
```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Orb Button`](/docs/components/orb-button)</Step>

</Steps>
</TabsContent>
</BlockTabs>