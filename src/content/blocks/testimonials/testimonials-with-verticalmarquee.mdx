---
title: Testimonials with Vertical Marquee
description: Vertical scrolling testimonial columns with alternating directions and smooth infinite animations.
---

<BlocksPreview
  name='testimonials-with-verticalmarquee'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/testimonials/testimonials-with-verticalmarquee
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Kinetic Testimonials`](/docs/components/kinetic-testimonials)</Step>

</Steps>
</TabsContent>
</BlockTabs>