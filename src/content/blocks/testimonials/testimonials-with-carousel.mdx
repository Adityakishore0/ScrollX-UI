---
title: Testimonials with Carousel
description: A testimonial carousel with staggered title animations and smooth transitions, showcasing customer feedback in an elegant layout.
---

<BlocksPreview
  name='testimonials-with-carousel'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/testimonials/testimonials-with-carousel
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge embla-carousel-react lucide-react
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Testimonial Carousel`](/docs/components/testimonial-carousel)</Step>

</Steps>
</TabsContent>
</BlockTabs>