---
title: Frequently asked questions Stack
description: A modern FAQ section with layered stack design and smooth scroll-triggered animations.
---

<BlocksPreview
  name='frequently-asked-questions-stack'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/frequently-asked-questions/frequently-asked-questions-stack
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge @radix-ui/react-progress
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Layer Stack`](/docs/components/layer-stack), [`Progress`](/docs/components/progress)</Step>

</Steps>
</TabsContent>
</BlockTabs>