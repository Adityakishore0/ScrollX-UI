---
title: Contact with Globe
description: Modern Contact with globe and smooth reveal animations.
---

<BlocksPreview
  name='contact-with-globe'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/contact-sections/contact-with-globe
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge lucide-react
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Button`](/docs/components/button), [`Seperator Pro`](/docs/components/seperatorpro), [`Globe Wireframe`](/docs/components/globe-wireframe)</Step>

</Steps>
</TabsContent>
</BlockTabs>