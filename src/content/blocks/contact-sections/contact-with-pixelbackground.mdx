---
title: Contact with Pixel Background
description: Minimal contact section with animated pixel background and stagger button.
---

<BlocksPreview
  name='contact-with-pixelbackground'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
```bash value="pkg"
npx shadcn@latest add @scrollxui/contact-sections/contact-with-pixelbackground
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>
```bash value="deps"
npm install motion clsx tailwind-merge lucide-react
```

<Step>Add util file</Step>
```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Button`](/docs/components/button), [`Stagger Button`](/docs/components/stagger-button), [`Pixel Background`](/docs/components/pixel-background)</Step>

</Steps>
</TabsContent>
</BlockTabs>