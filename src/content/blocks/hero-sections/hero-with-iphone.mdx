---
title: Hero Section with Iphone
description: A hero section with an iPhone mockup  and animated background.
---

<BlocksPreview
  name='hero-with-iphone'
  className=''
  description=''
/>


<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/hero-sections/hero-with-iphone
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge lucide-react
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Iphone`](/docs/components/iphone) , [`MovingLines Background`](/docs/components/movinglines-background)</Step>

</Steps>
</TabsContent>
</BlockTabs>