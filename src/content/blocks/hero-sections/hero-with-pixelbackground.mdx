---
title: Hero Section with Pixel Background
description: A hero-section for modern AI interfaces with motion.
---

<BlocksPreview
  name='hero-with-pixelbackground'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/hero-sections/hero-with-pixelbackground
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Pixel Background`](/docs/components/pixel-background), [`Timeline`](/docs/components/timeline)</Step>

</Steps>
</TabsContent>
</BlockTabs>