---
title: Hero Section with Reel
description: A hero section with an animated image reel and motion entrance.
---

<BlocksPreview
  name='hero-with-reel'
  className=''
  description=''
/>


<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
```bash value="pkg"
npx shadcn@latest add @scrollxui/hero-sections/hero-with-reel
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>
```bash value="deps"
npm install motion clsx tailwind-merge class-variance-authority lucide-react
```

<Step>Add util file</Step>
```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Reel`](/docs/components/reel) , [`Orb Button`](/docs/components/orb-button)</Step>

</Steps>
</TabsContent>
</BlockTabs>