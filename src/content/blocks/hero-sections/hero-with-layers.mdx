---
title: Hero Section with Layers
description: A hero-section for AI startups
---

<BlocksPreview
  name='hero-with-layers'
  className=''
  description=''
/>

<BlockTabs>
<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash value="pkg"
npx shadcn@latest add @scrollxui/hero-sections/hero-with-layers
```

</TabsContent>

<TabsContent value="manual">
<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash value="deps"
npm install motion clsx tailwind-merge @radix-ui/react-progress
```

<Step>Add util file</Step>

```ts filename="lib/utils.ts"
import { ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

<Step>Install required UI components: [`Layer Stack`](/docs/components/layer-stack), [`Progress`](/docs/components/progress), [`MovingLines Background`](/docs/components/movinglines-background)</Step>

</Steps>
</TabsContent>
</BlockTabs>