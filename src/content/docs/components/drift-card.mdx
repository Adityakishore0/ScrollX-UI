---
title: Drift Card
description: An interactive card with a drift hover effect that visually highlights content.
---

<ComponentPreview
  name="driftcard-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="An interactive card component works on hover effect"
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="drift-card" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>


<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>drift-card.tsx</code>
</p>
<ComponentSource name="drift-card" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>

```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import DriftCard from "@/components/ui/drift-card";
```

```tsx
<DriftCard className="w-full max-w-md h-72 mx-auto relative overflow-hidden">
      <img
        src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?q=80&w=1200&auto=format&fit=crop"
        alt="Preview"
        className="w-full h-full object-cover rounded-xl"
      />
    </DriftCard>
```

## API Reference

###  Drift Card

An interactive card with a drift hover effect that visually highlights content

## Props

<PropsTable
  rows={[
    {
      prop: "children",
      type: "ReactNode",
      default: "—",
      description: "Content rendered inside the drifting card."
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Additional classes to customize the container styling."
    },
    {
      prop: "style",
      type: "CSSProperties",
      default: "—",
      description: "Inline styles passed to the outermost container."
    },
    {
      prop: "...props",
      type: "HTMLAttributes<HTMLDivElement>",
      default: "—",
      description: "Any additional div attributes."
    }
  ]}
/>


