---
title: Animated Side Menu
description: AnimatedSideMenu is a responsive, theme-aware sidebar menu with smooth open/close animations and a glowing toggle button.
---

<ComponentPreview
  name="animatedsidemenu-demo"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[70%]"
  description="An accessible, animated sidebar menu with dark/light mode support"
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="animated-side-menu" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project:</Step>
      <p><code>components/ui/animated-side-menu.tsx</code></p>
      <ComponentSource name="animated-side-menu" />
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import AnimatedSideMenu from "@/components/ui/animated-side-menu";

export default function Demo() {
  return <AnimatedSideMenu />;
}
```

## API Reference

### AnimatedSideMenu

`AnimatedSideMenu` is a theme-aware sidebar component with smooth motion transitions, a glowing toggle button, and accessible navigation.

## Props

<PropsTable
  rows={[
      {
          prop: "open",
          type: "boolean",
          default: "false",
          description: "Internal state that controls whether the side menu is open."
      },
      {
          prop: "aria-expanded",
          type: "boolean",
          default: "false",
          description: "Accessibility attribute applied to the toggle button."
      },
      {
          prop: "className",
          type: "string",
          default: "undefined",
          description: "Optional custom class names for the wrapper container."
      }
    ]}
/>