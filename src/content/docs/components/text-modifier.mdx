---
title: Text Modifier
description: text highlighter with solid background and decorative markers for emphasis.
---

<ComponentPreview name="textmodifier-demo" className="" description="" />

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="text-modifier" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>text-modifier.tsx</code>
</p>
<ComponentSource name="text-modifier" />

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import TextModifier from "@/components/ui/text-modifier";
```

```tsx
<TextModifier
  variant="solid"
  highlightColorClass="bg-yellow-200"
  markerColorClass="bg-yellow-500"
  className="text-black dark:text-black"
>
  You can ask an AI assistant.
</TextModifier>
```

## API Reference

### Text Modifier

text highlighter with solid background and decorative markers for emphasis.

## Props

<PropsTable
  rows={[
    {
      prop: "highlightColorClass",
      type: "string",
      default: `"bg-yellow-200"`,
      description: "Background color class for the solid highlight of the text.",
    },
    {
      prop: "markerColorClass",
      type: "string",
      default: `"bg-yellow-500"`,
      description: "Color class for decorative top and bottom markers around the text.",
    },
    {
      prop: "opacity",
      type: "number",
      default: 0.6,
      description: "Opacity level of the highlight background.",
    },
    {
      prop: "animationDuration",
      type: "number",
      default: 0.6,
      description: "Duration of the highlight and marker animation in seconds.",
    },
    {
      prop: "animationDelay",
      type: "number",
      default: 0,
      description: "Delay before starting the highlight animation in seconds.",
    },
    {
      prop: "animate",
      type: "boolean",
      default: true,
      description: "Enable or disable animation for highlight and markers.",
    },
    {
      prop: "triggerOnView",
      type: "boolean",
      default: true,
      description: "Animate only when the component comes into viewport.",
    },
    {
      prop: "repeat",
      type: "boolean",
      default: false,
      description: "Allow animation to repeat when the component leaves and re-enters viewport.",
    },
    {
      prop: "padding",
      type: "string",
      default: `"0.125rem 0.375rem"`,
      description: "Padding around the text inside the highlight.",
    },
    {
      prop: "className",
      type: "string",
      default: `"optional"`,
      description: "Custom className to style the text content.",
    },
    {
      prop: "children",
      type: "ReactNode",
      default: "required",
      description: "Text or JSX content to be highlighted.",
    },
  ]}
/>

