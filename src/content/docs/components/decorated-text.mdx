---
title: Decorated Text
description: a decorative text frame with corner accents and a compact entrance effect.
---

<ComponentPreview
  name="decoratedtext-demo"
  className=""
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="decorated-text" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>decorated-text.tsx</code>
</p>
<ComponentSource name="decorated-text" />

<Step>Add util file</Step>

<p>
  <code>lib/utils.ts</code>
</p>

```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { DecoratedText } from "@/components/ui/decorated-text";
```

```tsx
<DecoratedText className="text-4xl font-bold">
    DecoratedText
  </DecoratedText>
```


## API Reference

### Decorated Text

a decorative text frame with corner accents and a compact entrance effect.

## Props

<PropsTable
  rows={[
    {
      prop: "text",
      type: "string",
      default: "-",
      description: "Text content to display. If omitted, `children` will be used."
    },
    {
      prop: "children",
      type: "ReactNode",
      default: "-",
      description: "React node content rendered when `text` is not provided."
    },
    {
      prop: "className",
      type: "string",
      default: "-",
      description: "Custom classes for styling the root element."
    },
    {
      prop: "bordered",
      type: "boolean",
      default: "true",
      description: "Toggles the border around the text."
    },
    {
      prop: "showIcons",
      type: "boolean",
      default: "true",
      description: "Controls visibility of corner icons."
    },
    {
      prop: "iconClassName",
      type: "string",
      default: "-",
      description: "Custom classes for styling each corner icon."
    },
    {
      prop: "glowEffect",
      type: "boolean",
      default: "true",
      description: "Enables the hover glow animation on the frame."
    },
    {
      prop: "flicker",
      type: "boolean",
      default: "true",
      description: "Controls the entrance flicker animation."
    }
  ]}
/>


