---
title: Card
description: Renders a card component comprising a header, content section, and footer...
---

<ComponentPreview
  name="card-demo"
  description="A subscription card for email and topic selection."
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="card" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>


<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>card.tsx</code>
</p>
<ComponentSource name="card" />

<Step>Add util file</Step>

<p>
  <code>lib/utils.ts</code>
</p>


```tsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>


## Usage
```tsx
import { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from "@/components/ui/card";
```

```tsx
<Card>
  <CardHeader>
    <CardTitle>Card Title</CardTitle>
    <CardDescription>This is a description of the card.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Some content for the card goes here.</p>
  </CardContent>
  <CardFooter>
    <Button variant="primary">Action</Button>
  </CardFooter>
</Card>
```

```tsx
<Card className="custom-card-style">
  <CardHeader>
    <CardTitle>Custom Card</CardTitle>
    <CardDescription>This card has a custom style.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Additional content with custom styles.</p>
  </CardContent>
  <CardFooter>
    <Button variant="link">Learn More</Button>
  </CardFooter>
</Card>
```

## API Reference

### Card

The `Card` component is used to create a customizable container with content sections such as title, description, and footer.

## Props

<PropsTable
  rows={[
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Optional custom class names. Used in: Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter."
    },
    {
      prop: "...props",
      type: "HTMLDivElement attributes",
      default: "—",
      description: "Applies to Card and all subcomponents. Inherits all standard div attributes and events."
    },
    {
      prop: "CardHeader",
      type: "ReactNode",
      default: "optional",
      description: "Header section of the card."
    },
    {
      prop: "CardTitle",
      type: "ReactNode",
      default: "optional",
      description: "Title inside the CardHeader."
    },
    {
      prop: "CardDescription",
      type: "ReactNode",
      default: "optional",
      description: "Description inside the CardHeader."
    },
    {
      prop: "CardContent",
      type: "ReactNode",
      default: "optional",
      description: "Main content area of the card."
    },
    {
      prop: "CardFooter",
      type: "ReactNode",
      default: "optional",
      description: "Footer section, typically for actions."
    }
  ]}
/>

