---
title: Dropdown
description: Animated Dropdown Reveals a menu of options or actions when activated by a button.
---

<ComponentPreview
  name="dropdownuser-demo"
  className=""
  description="Dropdown : Shows a list of choices on button click."
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="dropdown" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

    <Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>dropdown.tsx</code>
</p>
<ComponentSource name="dropdown" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>

```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import Dropdown, { MenuItem } from "@/components/ui/dropdown";
```

```tsx
<Dropdown
  menuItems={profileMenuItems}
  triggerText="User Menu"
  menuTitle="User Options"
  buttonVariant="outline"
  buttonSize="default"
/>
```

## Examples

---

<Step>Notifications</Step>

<ComponentPreview
  name="dropdownnotifications-demo"
  className=""
  description=""
/>

## API Reference

### Dropdown

The `Dropdown` component displays a clickable trigger that reveals a list of selectable menu items with icons and descriptions.

## Props

<PropsTable
  rows={[
    {
      prop: "menuItems",
      type: "Array<{ title: string; description?: string; icon?: React.ReactNode }>",
      default: "required",
      description: "Array of items to display in the menu."
    },
    {
      prop: "triggerText",
      type: "string",
      default: "required",
      description: "Text shown on the dropdown trigger button."
    },
    {
      prop: "menuTitle",
      type: "string",
      default: "—",
      description: "Optional title at the top of the menu."
    },
    {
      prop: "buttonVariant",
      type: `"default" | "outline" | "ghost" | string`,
      default: `"default"`,
      description: "Controls the visual style of the trigger button."
    },
    {
      prop: "buttonSize",
      type: `"default" | "sm" | "lg" | string`,
      default: `"default"`,
      description: "Controls the size of the trigger button."
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Optional custom class for the dropdown container."
    },
    {
      prop: "...props",
      type: "HTMLButtonElement attributes",
      default: "—",
      description: "Standard button props passed to the trigger."
    }
  ]}
/>

