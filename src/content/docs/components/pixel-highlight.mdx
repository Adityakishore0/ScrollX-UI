---
title: Pixel Highlight
description: Animated pixel-based text reveal with directional shimmer using canvas-rendered particles.
---

<ComponentPreview
  name="pixelhighlight-demo"
  className="overflow-visible relative p-0"
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="pixel-highlight" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>pixel-highlight.tsx</code>
</p>
<ComponentSource name="pixel-highlight" />

<Step>Add util file</Step>

<p>
  <code>lib/utils.ts</code>
</p>

```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { PixelHighlight } from '@/components/ui/pixel-highlight'
```

```tsx
<PixelHighlight
      text="Top Down"
      colors="#d8b4fe,#a78bfa,#7c3aed"
      direction="top"
      speed={70}
      gap={3}
      fontSize={12}
      className="w-full h-32"
    />
```


## API Reference

### Pixel Highlight

Animated pixel-based text reveal with directional shimmer using canvas-rendered particles.

## Props

<PropsTable
  rows={[
    {
      prop: "text",
      type: "string",
      default: "-",
      description: "Text to render as the pixel highlight animation."
    },
    {
      prop: "children",
      type: "node",
      default: "-",
      description: "Custom React content used instead of the text prop."
    },
    {
      prop: "colors",
      type: "string",
      default: "gradient",
      description: "Comma-separated color values randomly applied to animated pixels."
    },
    {
      prop: "gap",
      type: "number",
      default: "6",
      description: "Distance between pixels. Smaller values create denser highlights."
    },
    {
      prop: "speed",
      type: "number",
      default: "80",
      description: "Controls how fast pixels appear and shimmer."
    },
    {
      prop: "direction",
      type: "enum",
      default: "center",
      description: "Direction from which the pixel animation originates."
    },
    {
      prop: "opacity",
      type: "number",
      default: "1",
      description: "Overall transparency of the pixel canvas layer."
    },
    {
      prop: "fontSize",
      type: "string | number",
      default: "20",
      description: "Font size of the text mask. Numbers scale responsively."
    },
    {
      prop: "fontWeight",
      type: "string | number",
      default: "bold",
      description: "Weight of the text used for the pixel mask."
    },
    {
      prop: "fontFamily",
      type: "string",
      default: "sans",
      description: "Font family applied to the text mask."
    },
    {
      prop: "className",
      type: "string",
      default: "-",
      description: "Additional class names for the root container."
    }
  ]}
/>

