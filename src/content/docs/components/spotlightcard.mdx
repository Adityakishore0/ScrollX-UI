---
title: Spotlight Card
description: An interactive card with a spotlight hover effect that highlights content and enhances user engagement visually.
---

<ComponentPreview
  name='spotlightcard-demo'
  className='sm:[&_.preview>[data-orientation=vertical]]:max-w-[70%]'
  description='An interactive card component with spotlight hover effect'
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="spotlightcard" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name='motion' />

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name='spotlightcard' />

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { SpotlightCard } from '@/components/ui/spotlight-card';
```

```tsx
<SpotlightCard className='w-80 h-72' spotlightColor='34, 211, 238'>
  <div className='w-full h-full flex flex-col items-center justify-center text-center'>
    <h3 className='text-xl font-semibold mb-1'>Spotlight Card</h3>
    <p className='text-sm text-muted-foreground'>
      Hover to see the spotlight effect.
    </p>
  </div>
</SpotlightCard>
```

## API Reference

### SpotlightCard

The root component that creates the spotlight effect card.

## Props

| Prop             | Type        | Default          | Description                                      |
| ---------------- | ----------- | ---------------- | ------------------------------------------------ |
| `spotlightColor` | `string`    | `"14, 165, 233"` | RGB values for spotlight glow.                   |
| `className`      | `string`    | —                | Optional class for custom styling.               |
| `children`       | `ReactNode` | `optional`       | Content rendered inside the spotlight container. |
