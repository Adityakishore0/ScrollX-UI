---
title: Morphy Button
description: Morphing button with dynamic dot motion, reversible animation.
---

<ComponentPreview
  name="morphybutton-demo"
  className=""
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="morphy-button" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="@radix-ui/react-slot" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>morphy-button.tsx</code>
</p>
<ComponentSource name="morphy-button" />


<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>


## Usage

```tsx
import { MorphyButton } from "@/components/ui/morphy-button";
```

```tsx
<MorphyButton size="lg">
  Get Started
</MorphyButton>
```

## Examples

---

<Step>Reverse</Step>

<ComponentPreview
  name="morphybuttonreverse-demo"
  className=""
  description=""
/>

---

<Step>Size</Step>

<ComponentPreview
  name="morphybuttonsize-demo"
  className=""
  description=""
/>

---

<Step>Custom</Step>

<ComponentPreview
  name="morphybuttoncustom-demo"
  className=""
  description=""
/>


## API Reference

### Morphy Button

Morphing button with dynamic dot motion, reversible animation.

## Props

<PropsTable
  rows={[
    {
      prop: "size",
      type: `"sm" | "default" | "lg"`,
      default: `"default"`,
      description: "Controls button size and padding layout."
    },
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "Renders as a child element using Radix Slot if true."
    },
    {
      prop: "dotClassName",
      type: "string",
      default: "—",
      description: "Adds custom styling to the animated dot element."
    },
    {
      prop: "animate",
      type: `"normal" | "reverse"`,
      default: `"normal"`,
      description: "Reverses morph animation direction when set to 'reverse'."
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Custom Tailwind or CSS classes for additional styling."
    },
    {
      prop: "children",
      type: "ReactNode",
      default: "—",
      description: "Button label or nested elements."
    },
    {
      prop: "...props",
      type: "button props",
      default: "—",
      description: "Inherits all native HTML button attributes and events."
    }
  ]}
/>

