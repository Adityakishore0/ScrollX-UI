---
title: Stagger Button
description: Interactive button with animated stagger text and customizable styles for modern uis.
---

<ComponentPreview
  name="staggerbutton-demo"
  className=""
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="stagger-button" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion @radix-ui/react-slot" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>stagger-button.tsx</code>
</p>
<ComponentSource name="stagger-button" />


<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>


## Usage

```tsx
import { StaggerButton } from "@/components/ui/stagger-button";
```

```tsx
<StaggerButton direction="up">
     Get started
  </StaggerButton> 
```

## Examples

---

<Step>Up</Step>

<ComponentPreview
  name="staggerbuttonup-demo"
  className=""
  description=""
/>

---

<Step>Alternate Even</Step>

<ComponentPreview
  name="staggerbuttoneven-demo"
  className=""
  description=""
/>

---

<Step>Alternate Odd</Step>

<ComponentPreview
  name="staggerbuttonodd-demo"
  className=""
  description=""
/>


## API Reference

### Stagger Button

Interactive button with animated stagger text and customizable styles for modern uis.

## Props

<PropsTable
  rows={[
  {
    "prop": "variant",
    "type": "string",
    "default": "default",
    "description": "Button style (default, destructive, outline,..)"
  },
  {
    "prop": "size",
    "type": "string",
    "default": "default",
    "description": "Button size (default, sm, lg, icon)"
  },
  {
    "prop": "asChild",
    "type": "boolean",
    "default": false,
    "description": "Renders as Slot child element"
  },
  {
    "prop": "text",
    "type": "string",
    "default": "—",
    "description": "Base button text"
  },
  {
    "prop": "hoverText",
    "type": "string",
    "default": "—",
    "description": "Text displayed on hover"
  },
  {
    "prop": "staggerDelay",
    "type": "number",
    "default": 0.05,
    "description": "Delay between each character animation"
  },
  {
    "prop": "staggerDuration",
    "type": "number",
    "default": 1,
    "description": "Duration of the stagger animation"
  },
  {
    "prop": "direction",
    "type": "string",
    "default": "down",
    "description": "Animation direction (up, down, alternate-even, alternate-odd)"
  },
  {
    "prop": "easing",
    "type": "number[]",
    "default": "[0.22, 1, 0.36, 1]",
    "description": "Easing curve for animation timing"
  },
  {
    "prop": "disableStagger",
    "type": "boolean",
    "default": false,
    "description": "Disables stagger text animation"
  },
  {
    "prop": "className",
    "type": "string",
    "default": "—",
    "description": "Custom CSS classes for styling"
  },
  {
    "prop": "children",
    "type": "ReactNode",
    "default": "—",
    "description": "Button content: text or elements"
  },
  {
    "prop": "...props",
    "type": "button props",
    "default": "—",
    "description": "Supports native button attributes and events"
  }
]}
/>

