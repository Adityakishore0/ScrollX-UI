---
title: FlipFlow
description:  A looping flow of flip cards with smooth motion and subtle interactive effects.
---

<ComponentPreview
  name="flipflow-demo"
  className="p-0"
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="flipflow" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>flipflow.tsx</code>
</p>
<ComponentSource name="flipflow" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx 
import {(ClassValue, clsx)} from "clsx";
import {twMerge} from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

<Step>Add these animations to your `tailwind.config.ts:`</Step>
<p>
  <code>tailwind.config.ts</code>
</p>
```ts
// tailwind.config.ts
export default {
  theme: {
    extend: {
      keyframes: {
        "canopy-x": {
          from: { transform: "translateX(0)" },
          to: { transform: "translateX(calc(-100% - var(--gap)))" },
        },
        "canopy-y": {
          from: { transform: "translateY(0)" },
          to: { transform: "translateY(calc(-100% - var(--gap)))" },
        },
      },
      animation: {
        "canopy-horizontal": "canopy-x var(--duration) infinite linear",
        "canopy-vertical": "canopy-y var(--duration) linear infinite",
      },
    },
  },
}
```

## Usage

```tsx
import { FlipFlow } from "@/components/ui/flipflow";
const keyData = [
    { name: "SCROLLX UI" },
    { name: "SCROLLX UI" },
    { name: "SCROLLX UI" },
    { name: "SCROLLX UI" },
    { name: "SCROLLX UI" },
    { name: "SCROLLX UI" },
  ];
```

```tsx
<FlipFlow 
  data={keyData}
  cardClassName="h-20 w-48"
/>
```


## API Reference

###  FlipFlow

A looping flow of flip cards with smooth motion and subtle interactive effects.

## Props

<PropsTable
  rows={[
    {
      prop: "data",
      type: "Array<{ name: string }>",
      default: "required",
      description: "Array of card objects to render inside the flow."
    },
    {
      prop: "name",
      type: "string",
      default: "required",
      description: "Text displayed on both the front and back of each flip card. Used inside the data object."
    },
    {
      prop: "className",
      type: "string",
      default: "-",
      description: "Additional CSS classes applied to the FlipFlow container."
    },
    {
      prop: "cardClassName",
      type: "string",
      default: "-",
      description: "Additional CSS classes applied to each flip card."
    },
    {
      prop: "colors",
      type: "string[]",
      default: "preset gradients",
      description: "Gradient background classes applied to the front side of cards. Cycles through the array."
    },
    {
      prop: "backColors",
      type: "string[]",
      default: "preset gradients",
      description: "Gradient background classes applied to the back side of cards. Cycles through the array."
    }
  ]}
/>




