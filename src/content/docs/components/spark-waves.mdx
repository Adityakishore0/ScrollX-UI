---
title: Spark Waves
description: Radial spark waves pulse outward and inward, creating a dynamic energy-driven background layer.
---

<ComponentPreview
  name="sparkwaves-demo"
  className="p-0"
  description=""
/>

## Installation

<Tabs defaultValue="cli" className="w-full">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    <PkgOptions name="spark-waves" />
  </TabsContent>
  <TabsContent value="manual">
    <Steps>

<Step>Install the following dependencies:</Step>
<DepsOptions name="framer-motion" />

<Step>Copy and paste the following code into your project.</Step>
<p>
  <code>spark-waves.tsx</code>
</p>
<ComponentSource name="spark-waves" />

<Step>Add util file</Step>
<p>
  <code>lib/utils.ts</code>
</p>
```jsx
import { ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

</Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { SparkWaves } from "@/components/ui/spark-waves";
```

```tsx
<SparkWaves
      sparkColor="#818cf8"
      sparkSize={12}
      sparkCount={28}
      duration={4500}
      waveInterval={1600}
      maxRadius={900}
      ringsPerWave={7}
      ringSpacing={55}
      enableInward={true}
      className="w-full h-[350px]"
    >
      <h2 className="bg-clip-text text-transparent text-center bg-gradient-to-b from-neutral-900 to-neutral-700 dark:from-neutral-600 dark:to-white text-2xl md:text-4xl lg:text-7xl font-sans py-2 md:py-10 relative z-20 font-bold tracking-tight">
        Spark Waves Animation, <br /> Background.
      </h2>
      </SparkWaves>
```
## Examples

---

<Step>Filled Variant</Step>

<ComponentPreview
  name="barswavefilled-demo"
  className="p-0"
  description=""
/>

## API Reference

### BarsWave

Radial spark waves pulse outward and inward, creating a dynamic energy-driven background layer.

## Props

<PropsTable
  rows={[
    {
      prop: "sparkColor",
      type: "string",
      default: '"#3b82f6"',
      description: "Color of the sparks and glow effect.",
    },
    {
      prop: "sparkSize",
      type: "number",
      default: "10",
      description: "Length of each animated spark line.",
    },
    {
      prop: "sparkCount",
      type: "number",
      default: "24",
      description: "Number of sparks per ring.",
    },
    {
      prop: "duration",
      type: "number",
      default: "4000",
      description: "Animation duration per wave in milliseconds.",
    },
    {
      prop: "easing",
      type: "string",
      default: '"ease-out"',
      description: "Easing curve applied to spark motion.",
    },
    {
      prop: "waveInterval",
      type: "number",
      default: "1400",
      description: "Delay between consecutive spark waves.",
    },
    {
      prop: "maxRadius",
      type: "number",
      default: "1000",
      description: "Maximum expansion radius of sparks.",
    },
    {
      prop: "ringsPerWave",
      type: "number",
      default: "6",
      description: "Number of rings emitted per wave.",
    },
    {
      prop: "ringSpacing",
      type: "number",
      default: "50",
      description: "Spacing between each spark ring.",
    },
    {
      prop: "enableInward",
      type: "boolean",
      default: "true",
      description: "Enables inward moving spark waves.",
    },
    {
      prop: "className",
      type: "string",
      default: "—",
      description: "Custom classes for the container.",
    },
    {
      prop: "children",
      type: "node",
      default: "—",
      description: "Content rendered above the animation.",
    },
  ]}
/>



