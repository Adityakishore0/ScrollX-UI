import { StripeButton } from "@/components/ui/stripe-button";

export default function StripeButtonDemo() {
  return (
    <StripeButton className="italic" variant="compact">
      Enter the Arena!
    </StripeButton>
  );
}
